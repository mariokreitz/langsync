---
'@mariokreitz/langsync': patch
---

Detect `i18next-vue` (the maintained Vue 3 binding for i18next) during
`langsync init`, alongside the existing `i18next` / `react-i18next` /
`vue-i18next` signatures.
